# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'L105SwiftUI' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for L105SwiftUI
  pod 'Alamofire'

  target 'L105SwiftUITests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'L105SwiftUIUITests' do
    # Pods for testing
  end

end
